# Pre-existing errors log

Bugs/type-errors Claude noticed while working on an unrelated task, in files it did not intend to change, and did not fix. Each entry is a candidate for its own small follow-up task — pick one when convenient.

Convention: newest entries at the top. Include date found, file:line, the exact error, and enough context for someone to pick it up cold.

---

## 2026-09-23 — SMS-receipt snapshot builder reads `store.zipCode`, a field that doesn't exist (the column is `postcode`) — `artifacts/api-server/src/routes.ts:19285`

**Found while:** Redesigning the printed thermal-receipt layout (a separate feature) and its store-address plumbing. Found the identical `zipCode` typo in `artifacts/booking/src/pages/Calendar.tsx`'s printed-receipt call site and fixed it there as part of that task, then noticed this second, unrelated occurrence.
**Symptom:** `storeAddress: [(store as any)?.address, (store as any)?.city, (store as any)?.state, (store as any)?.zipCode].filter(Boolean).join(", ")` — `locations`/`Store` (`shared/schema.ts:198`) has a `postcode` column, not `zipCode`. `(store as any)?.zipCode` is always `undefined`, so the zip is silently dropped from the address stored in `appointments.receipt_snapshot`, used by the "text me a receipt" SMS flow.
**Why not fixed now:** Out of scope — this endpoint builds the snapshot for the *texted/SMS* receipt, not the *printed* thermal receipt this task was about. One-line fix: `zipCode` → `postcode`.

---

## 2026-09-23 — Central error handler logs via `console.error` instead of the pino logger — `artifacts/api-server/src/index.ts:~1681-1692`

**Found while:** Rentals Phase-0 architecture audit (read-only investigation of the whole backend, no changes made). Not part of the audit's scope to fix.
**Symptom:** The app has a structured pino logger (`artifacts/api-server/src/lib/logger.ts`, wired in via `pino-http` in `app.ts`, with redaction of auth headers/cookies) used everywhere else. The final Express error-handling middleware near the end of `index.ts` (right before `httpServer.listen`) instead calls `console.error(err)` directly, so unhandled-route errors bypass the redaction/formatting/log-aggregation pipeline the rest of the app relies on.
**Why not fixed now:** Purely an audit task — instructed not to modify any code. Low severity (doesn't leak more than console.error already would, just inconsistent), but worth a one-line follow-up (swap to `logger.error(...)`) whenever someone is next in that file.

---

## 2026-09-23 — Duplicate numeric prefixes in `/apps/CM/migrations/` (0000, 0001, 0025, 0123, 0132 each appear twice)

**Found while:** Rentals Phase-0 architecture audit, mapping the migration directory to determine a safe strategy for future Rentals migrations.
**Symptom:** The migrations directory uses a 4-digit zero-padded sequential numbering convention (`NNNN_description.sql`), currently running up to `0201`. At least five prefixes (0000, 0001, 0025, 0123, 0132) each have two different files with the same number but different descriptive suffixes — apparently left over from historical branch merges. `runMigrations.ts` tracks applied-by-filename (not by number) in a `schema_migrations` table, so both files with the same number do get applied (no data loss), but the duplication makes the sequence ambiguous to a human scanning the directory and risks a future real collision if someone picks the "next" number by eyeballing the highest duplicate.
**Why not fixed now:** Out of scope for an audit-only task, and renaming existing applied migration files is risky (the tracking table is keyed by filename) — not something to do without deliberate, careful follow-up and coordination with whoever owns the deploy pipeline.

---

## 2026-09-22 — ✅ FIXED — Tenant-isolation sweep, final batch: `routes/*.ts` files outside `routes.ts`/`clients.ts`

**Found while:** the user's directive to make sure no salon can see/access another's data. A first attempt to audit all ~50 remaining route files in one agent pass got stuck and never returned (30-45+ min with no result); split into 5 narrower, faster audits (payroll/commerce, billing/dashboard, media/GBP, crm-search/plans, and a broad misc sweep of ~27 low-risk files) which all completed normally.
**Severe findings, now fixed:**
- **`dataTransfer.ts` — mass-delete via rollback.** `POST /jobs/:id/rollback` deleted clients/appointments/services/products/gift cards by IDs stored in a job row, with zero check that the job belonged to the caller's store — any authenticated user could guess/enumerate a job id from any store and mass-delete its data. `POST /start`, `GET /jobs`, `GET /jobs/:id`, `POST /jobs/:id/execute` had the same missing-ownership pattern (create fake import jobs into another store, read another store's job preview data with PII). All 5 routes now verify `job.store_id`/resolve via `resolveSessionStoreId`.
- **`contractorPayouts.ts` — systemic, 17 routes.** A local `storeId(req)` helper trusted `req.query.storeId`/`req.body.storeId` with zero validation (unlike the file's own correctly-built `resolveSessionStoreId` import, already used elsewhere in the same file) — read/write access to another store's full payout ledger, checks, audit log, financial reports, and a write path that could inject a fake commission ledger adjustment into another store's books. Fixed by delegating the helper to `resolveSessionStoreId` (fixes 10 routes at once: runs/checks/overview/hub-summary/ledger/ledger-adjustment/contractor-adjustments/audit-logs/reports/onboarding-token-statuses). Plus 7 more individually-unscoped routes: `GET /runs/:id`, `POST /runs/:id/cancel`, `POST /checks/:id/{mark-printed,void,mark-cleared}`, `DELETE /contractors/:id`, `GET /contractors/:id/bank-accounts` (leaked bank name/last4/routing), `GET /w9/:contractorId` (leaked legal name, business name, tax-ID last4, address).
- **`routes/payroll.ts`** (legacy, confirmed dead — never imported/mounted anywhere) — same `storeId`-from-body pattern on 2 routes. Fixed anyway for defense-in-depth in case it's ever remounted.
- **`crm-search.ts` — cross-tenant PII search.** `GET /` took `store_id` from the query string, only checked the store *existed* (never that the caller owned it) — any authenticated user could read another store's full client list (names/emails/phones), staff roster, appointments, services, and product inventory just by changing the query param. Now uses `resolveSessionStoreId`.
- **`plans.ts`** — `my-features`/`my-plan` trusted a `storeId` query/body/session value in that priority order with no ownership check, leaking another store's subscription status and Stripe subscription ID. Now uses `resolveSessionStoreId`.
- **`sync.ts` — `POST /queue`** let a client-supplied `storeId` in the body override the session-resolved store for every downstream write (create/cancel appointments, check-in/out, edit clients). Now always uses the session-derived store; the body override was removed entirely.
- **`illustrationCategories.ts`** — `GET /usage` and `POST /bulk-auto-assign` trusted `storeId` from query/body; the bulk route could reassign illustration categories across another store's entire service catalog. Now uses `resolveSessionStoreId`.
- **`intelligence-demo.ts` — destructive cross-tenant reseed.** `GET /launch` and `GET /status` took `storeId` from the query with only an account-type gate (tester/demo emails), no check the demo account owned that store — `/launch` calls a seeder that deletes and reseeds a store's clients + appointments, so a demo account could point it at a real production store's id and wipe its data. Now both verify the resolved session store matches the requested id.
- **`crew-mobile.ts` — unauthenticated PIN takeover.** `POST /set-pin` had literally no auth middleware at all (its own comment incorrectly claimed it "uses store session") — anyone could set a crew member's login PIN by guessing a `crewId`+`storeId` pair, then log in as them. Now requires `isAuthenticated` and verifies the crew belongs to the caller's resolved store. (No live frontend caller currently exists — the only reference is an orphaned, unbuilt `pro-dashboard-backup/` copy — so this closes a real but not-yet-exercised hole.)
- **`pro-dashboard.ts`** — `DELETE /orders/:id` and `POST /orders/:id/notes` were the only two routes in this otherwise-correctly-scoped file that didn't verify the order belonged to the caller's store before deleting its notes / attaching a new one.
- Confirmed clean, no changes needed: `galleryPhotos.ts`, `imageLibrary.ts`, `serviceImages.ts`, `serviceImport.ts`, `staffWorkPhotos.ts`, `gbpPhotoEngine.ts`, `gbpPostEngine.ts`, `googleReviewEngine.ts`, `reviewGating.ts`, `websites.ts`, `subdomains.ts`, `billing.ts`, `subscription.ts`, `stripeConnect.ts`, `dashboard.ts`, `manage.ts`, `onboarding.ts`, `usage.ts`, `calendarSync.ts`, `commissionReserves.ts`, `marketplaceDealsApi.ts`, `salonApi.ts`, `payrollRuns.ts`, plus ~20 low-risk utility/webhook/admin-gated files (health, translations, validate, systemStatus, contact, unsubscribe, support*, liveChat, blog, sync-jobs, agentAccounts, aiReceptionist, intelligence.ts, templates, setup, platformEmailCampaigns, emailPreferences, dashboardWS, stripeConnectWebhook, stripeWebhook, index.ts).
**Separate, non-tenant-isolation finding (not fixed, logged for later):** `imageLibrary.ts`'s list/upload/harvest routes have no `isAuthenticated` at all. Not a cross-tenant leak (that table has no per-store data — it's an intentionally shared platform image library) but a resource-abuse vector (anyone could upload to or harvest the shared library). Low priority; own follow-up.
`tsc --noEmit` clean across all of `artifacts/api-server` after every batch in this sweep.

**Combined with the routes.ts (26+ routes) and clients.ts (16 routes) fixes earlier the same day, this closes the full cross-tenant data-isolation sweep the user asked for.**

---

## 2026-09-22 — ✅ FIXED — `POST /api/staff/:id/avatar` and `PUT /api/staff/:id/pay-rate` had no store-scoping

**Found while:** restoring the staff-photo-upload UI to `/team/:id` (`StaffDetail.tsx`), which calls the avatar endpoint.
**Was:** `routes.ts` (avatar ~4599, pay-rate ~18740) took `staffId` straight from the URL and updated it with only `isAuthenticated` checked — no verification the target staff row belongs to the caller's own store.
**FIXED 2026-09-22**, as part of a full cross-tenant-isolation sweep the user asked for after this was flagged ("make this codebase so no other salon can see or access another salon's data"): both now resolve the caller's store via `resolveSessionStoreId` and verify the target staff row's `storeId` matches before proceeding (avatar route skips the check for the `me` self-upload alias, which is already session-bound). Part of a much larger sweep this session — see the `MEMORY.md`-tracked tenant-isolation audit for the full list of ~30+ routes fixed across `routes.ts` (timeclock PINs, unauthenticated `/api/admin/stores/:storeNumber/*` endpoints leaking a plaintext Mailgun API key, payroll runs, Google Business OAuth hijack, gift card tampering, team role/permission escalation, intake forms, reviews, waitlist, and more). `tsc --noEmit` clean throughout.

---

## 2026-09-22 — ✅ FIXED — `/frontdesk` cross-talk: kiosk_checkout_tip_request / await_payment / payment_result weren't guarded by appointmentId

**Found while:** fixing the reported bug where a second, unrelated ticket starting checkout on the same register overwrote the customer-facing `/frontdesk` display mid-transaction (`kiosk_checkout_start`/`cart` fixed same day with a `posApptIdRef`-based guard).
**Was:** the other three checkout-flow WS cases — `kiosk_checkout_tip_request`, `kiosk_checkout_await_payment`, `kiosk_checkout_payment_result` — applied no equivalent guard, and `tip_request`/`payment_result` didn't even carry an `appointmentId` in their payload.
**FIXED 2026-09-22:** added `appointmentId` to every `tip_request`/`payment_result` send call site (`Calendar.tsx`'s `CheckoutPOSPanel` uses `appointment.id`; its `WalkInCheckoutPanel` uses `0`, the existing sentinel for "no ticket yet"; `CheckoutMode.tsx` and `useCardPayments.ts` use `ticket.id`/`o.ticketId` — 12 call sites total). `FrontDeskDisplay.tsx` now applies the same `posApptIdRef` guard to all three cases that `kiosk_checkout_start`/`cart` already had. `tsc --noEmit` clean on the booking package.

---

## 2026-09-22 — ✅ FIXED — Salon directory: claimed-only noindex/aggregateRating gating was deliberately deleted, re-indexing the entire unclaimed directory

**Found while:** running `/geo-audit certxa.com`.
**FIXED 2026-09-22 (same day, user asked to fix the logged findings):** restored the `isVerified` gate in `entry-server.tsx` (`noindex, follow` for unclaimed salons, and `aggregateRating` is now also gated on `isVerified` — not just `rating>0`), and switched both sitemap routes in `salonDirectory.ts` back to `await getClaimedSalonList()`. `tsc --noEmit` clean on both packages.
**Was fixed once:** commit `4fcdf767` (2026-09-08) added claim-status gating: unclaimed `/salon/:slug` pages got `noindex, follow` and were excluded from `/salon/sitemap*.xml` (only `getClaimedSalonList()` results were listed/indexed).
**Regressed:** commit `2acebbf5` (2026-09-15, "marketplace: fix GEO audit findings — sitemap, schema, disclosure") deliberately reversed this. `artifacts/marketplace/src/entry-server.tsx:397-402` now calls `baseHead(..., 'index, follow, ...')` unconditionally for every resolved slug (the `isVerified` ternary was deleted). `artifacts/api-server/src/routes/salonDirectory.ts:128-174`'s `/sitemap-salons.xml` / `/sitemap-salons-:page.xml` now call `getSalonList()` (the full ~47-55k record dataset) instead of `getClaimedSalonList()` — line 180's own comment says "all real records, not just claimed listings — see GEO-AUDIT-REPORT.md," i.e. the regression was intentional, apparently on a misreading of the Sep-8 policy as a bug. `getClaimedSalonList()` (`artifacts/api-server/src/lib/salonData.ts:263-287`) still exists but is now only consumed by `salonApi.ts` search ranking — dead code with respect to indexing/schema decisions.
**Effect, confirmed live 2026-09-22:** ~10,000 sampled directory listing URLs (of a much larger underlying corpus) all serve `index, follow` plus a full `aggregateRating` schema block (e.g. `ratingValue: 4.8, reviewCount: 313`) on unclaimed, unverified third-party businesses with boilerplate templated description text and no visible review content backing the count — verified identical pattern on 3 independently sampled pages. This is a real-at-scale Google structured-data policy risk and a plausible driver of a sitewide quality/trust discount, not just a GEO nice-to-have.
**Suggested fix:** restore the `isVerified` gate in `entry-server.tsx`'s head-tag branch and switch the sitemap routes back to `getClaimedSalonList()`, or make an explicit, reviewed product decision to index the full directory (in which case the `aggregateRating` assertion on unclaimed listings still needs its own fix — see GEO-AUDIT-REPORT.md).

---

## 2026-09-22 — ✅ FIXED — `/nail` and `/calendar` (no trailing slash) serve a crawlable, indexable SPA shell with no robots.txt rule

**Found while:** running `/geo-audit certxa.com`.
**Was:** `robots.txt` disallows `/calendar/` (trailing slash) and other internal-app prefixes, but `curl https://certxa.com/nail` and `curl https://certxa.com/calendar` (no trailing slash) both return HTTP 200 with `<meta name="robots" content="index, follow">` — the booking/staff SPA's static `index.html` served on a catch-all route, not blocked by any existing robots.txt line. Turned out to be systemic, not specific to these two paths: every one of the ~80 `KNOWN_APP_PREFIXES` routes (dashboard, staff, pos, settings, etc.) falls through to the same catch-all and got the same hardcoded `index, follow` from the raw `index.html`.
**FIXED 2026-09-22:** added `injectNoindex()` in `artifacts/api-server/src/static.ts` (mirrors the existing `injectSeoMetadata()` pattern) and applied it in the final SPA catch-all — every route reaching that point (real public pages are always handled earlier by PHP/SSR/SEO_CONFIG) now serves `noindex, nofollow` instead of the shell's default `index, follow`.

---

## 2026-09-22 — ✅ FIXED — Homepage demo dashboard shows a client "since Mar 2024," two years before Certxa's real Feb 2026 founding date

**Found while:** running `/geo-audit certxa.com`.
**Was:** `php/overview/default.php:368` — `<div class="fvc-name">Emma Clarke</div><div class="fvc-meta">Client since Mar 2024 · 24 visits</div>` in the homepage hero/demo mockup (confirmed live: `php/index.php:13` requires this file as the homepage). Same "Emma Clarke" mock data (without the conflicting date) also appears in `php/client-management/default.php:46,51`, `php/online-booking/default.php:91,425`, `php/salonos/default.php:345,376`, `php/client-notifications/default.php:50` — those don't assert a date, so left alone.
**FIXED 2026-09-22:** changed to "Client since Apr 2026" — after the real Feb 2026 founding date.

---

## 2026-09-22 — ✅ FIXED — API request logger writes full JSON response bodies to the pm2 log, including live Stripe Terminal connection-token secrets

**Found while:** reading `/root/.pm2/logs/certxa-api-out-9.log` to debug the Nail POS card button (2026-09-21). Nothing was changed that day.
**Was:** `artifacts/api-server/src/index.ts` appended `JSON.stringify(capturedJsonResponse)` to every `/api` log line, unredacted — e.g. `POST /api/payments/terminal/connection-token :: {"secret":"pst_live_…"}`, plus client secrets, session/login responses and client phone numbers on any other endpoint.
**FIXED 2026-09-22:** new `lib/logRedaction.ts` — `/api/payments/*` and `/api/auth/*` responses are never captured at all ("`:: [body not logged]`"); every other response is walked recursively and any key matching secret/token/password/pin/phone/email/card-number/etc. (case-insensitive) has its VALUE replaced with `"[redacted]"`, however deep it's nested; the logged JSON is capped at 500 chars so one huge payload can't flood the log. `index.ts`'s logger now calls `logBodySuffix(path, body)` instead of stringifying the raw body. Tests: `__tests__/logRedaction.test.ts` (11 checks: skip-prefixes, nested/array redaction, ordinary fields untouched, depth-guard, truncation).

---

## 2026-09-21/22 — ✅ FIXED — Loyalty program audit (store 2 / Toby Benjamin): duplicate award, ledger drift, unscoped adjust, one-shot rewards fetch

**Found while:** auditing why client Toby Benjamin (clients.id 17, store 2, 10 pts/$) shows 5,345 pts. Nothing was changed. His balance = 4,352 (correct 10 pts/$ earnings on $435.15) + 550 (appt #82 awarded twice) + 190 (July, earned at the old 1 pt/$ rate) + 1 (absorbed from merged duplicate client 82) + 252 (balance with no ledger row, source untraceable). Ledger sum is 5,092; only this one client in store 2 has balance != ledger.
1. **`lib/loyaltyAward.ts` `awardLoyaltyForCompletion` is not idempotent.** It inserts an `earn` row every call. Reopening a completed ticket (completed → started, appointment_events #46) and completing it again awards again: appt #82 got 550 pts at 08:30 and 550 at 11:37 on 2026-08-31 (the only double-award in the DB). `routes.ts` only guards `wasAlreadyCompleted` for Terminal-capture races, not reopen → complete. Fix idea: skip if an `earn` row exists for that appointment_id (or reverse the award when a ticket is reopened).
2. **Earn basis is `totalPaid`, not service price** (`loyaltyAward.ts:31`): tips and gift-card / discounted amounts earn points (appt #113: $1.15 paid incl. $0.15 tip → 12 pts). Product decision, but it is why pts ≠ 10 × service price.
3. **Client merge (`routes/clients.ts` ~936) drifts balance from the ledger.** It adds the loser's `loyalty_points` to the winner with no ledger row, does not re-point `loyalty_transactions.customer_id` (ledger row 5, appt #76, still belongs to archived client 82 while the appointment now belongs to 17), and leaves the loser's balance non-zero (client 82 archived, still shows 1 pt). `client_audit_logs` records only field NAMES for edits, never values, so the unexplained +252 on client 17 cannot be traced. `storage.ts` 771/856 also let create/update set `loyaltyPoints` directly with no ledger row.
4. **`POST /api/loyalty/adjust` (`routes.ts`) is not store-scoped or validated:** `customerId`/`storeId` come from the request body, only `isAuthenticated` is checked, `type` is free text, `points` unvalidated (NaN/huge). Any signed-in user can adjust any store's client.
5. **`POST /api/loyalty/redeem` is read-then-write (not atomic)** — two stations redeeming at once can both pass the balance check. Separately, the Nail POS `completeTicket` (`booking/.../nail/nailApi.ts`) deliberately swallows a failed redeem (`.catch(() => {})`), so the sale can close with the reward discount applied but the points not deducted.
6. **`FrontDeskDisplay.tsx` ~316 fetches `/api/public/kiosk/:slug/rewards` once on mount and swallows errors** — one failed request (deploy/restart/network blip) leaves "No rewards available right now." until the tablet page is reloaded, even though the endpoint returns the rewards (verified live for `jims`). The same public endpoint ignores `loyalty.enabled` (returns rewards even if the program is off).
7. **`clients.total_spent_cents` / `total_visits` look stale:** Toby shows 191 cents ($1.91) and 5 visits vs $626.15 actually paid over 11 completed tickets.
**RESOLVED the same day ("fix all of it"):** (1) `lib/loyaltyAward.ts` rewritten — awards book only the difference between what an appointment should have earned and what it already earned, under a client-row lock, so reopen → complete / retries / racing paths can't double-award; (2) tips no longer earn points (basis = totalPaid − tip; one-line change in `pointsToAward` if the salon wants tips to count); (3) client merge now goes through `mergeLoyaltyTx` (re-points the ledger, carries + zeroes the loser's balance, books untracked points as an "adjust" row) and writes total_spent_cents in cents; (4) `/api/loyalty/adjust` uses the session store, validates type/sign/size, and books what was actually applied (never below 0); (5) `/api/loyalty/redeem` is atomic and once-per-ticket, and the Nail POS `completeTicket` now fails the sale if the redeem fails (safe to retry); (6) `/frontdesk` retries the rewards fetch and refreshes it when a checkout opens; the public rewards endpoint returns [] when the program is off; (7) cached total_visits / total_spent_cents are refreshed on every completion (last_visit_at deliberately NOT touched — the lapsed-client scheduler keys off it) and were backfilled. Data: Toby's duplicate 550 pts reversed (5,345 → 4,795), the merged duplicate's ledger row moved, 252 untracked points recorded as an opening-balance row; every client in every store now has balance == ledger. Backup of the touched rows: scratchpad `loyalty-backup-before.json` (session only). Tests: `__tests__/loyaltyAward.test.ts` + a self-cleaning DB integration run (15 checks, incl. 3 simultaneous completions and 2 racing redeems).
**FIXED 2026-09-22:** `POST /api/clients/merge` now resolves `storeId` from the session (`resolveSessionStoreId`, the same ownership-checked helper `routes.ts` already uses elsewhere) instead of trusting the request body; every scoping check inside the handler already used that variable, so this closes the hole with a 6-line change. No caller exists yet in `artifacts/booking/src` — nothing to break.
**FIXED 2026-09-22** (part of the full cross-tenant-isolation sweep — "make this codebase so no other salon can see or access another salon's data"): a full read-through of `routes/clients.ts` found this was much wider than the original 9-route estimate — **16 routes** trusted a client-supplied `storeId` or had no ownership check on a `:id`-scoped resource at all, including several severe ones the initial sweep missed entirely: `GET /:id` (returned a client's ENTIRE profile — emails, phones, addresses, notes, custom fields, loyalty points — for any id, cross-tenant), `PATCH /:id` and `DELETE /:id` (edit/archive any client cross-tenant, no check), `POST /export` (full customer-database CSV/XLSX/JSON export — name/email/phone/address/notes — for any store just by naming its id), `POST /import/execute` (inject fake client records into another store), `POST /detect-duplicates` (probe another store's client PII by email/phone/name), plus notes (`GET`/`POST /:id/notes`, `PATCH`/`DELETE /:id/notes/:noteId`), the AI `profile-note` routes, `POST /:id/phones`, `POST /:id/emails`, `GET`/`PUT /:id/marketing-preferences`, `POST`/`DELETE /:id/tags`, `GET /find-all-duplicates`, `GET /tags/list`, `GET /audit-logs`, `GET /:id/appointments`, the two `phone-types/*` routes, and the client list (`GET /`) and create (`POST /`) routes. All now resolve the caller's store via `resolveSessionStoreId` and verify any `:id`-scoped resource actually belongs to it before reading/writing. Frontend callers (`ExportDialog.tsx`, `ImportDialog.tsx`) already send their own `selectedStore.id`, which `resolveSessionStoreId` validates and accepts, so multi-location owners switching stores are unaffected. `tsc --noEmit` clean.

---

## 2026-09-22 — ✅ FIXED — Legacy `/api/gift-cards/check` + `/redeem`: unauthenticated lookup, no store scoping, non-atomic, unvalidated amount

**Found while:** adding gift cards as a Nail POS payment (2026-09-21). **Correction to that day's note:** it said "nothing in `artifacts/booking/src` calls them" — that was wrong. `pages/GiftCards.tsx` (the owner's Gift Cards console, "Check / Redeem" card — a phone order or a manual balance correction, no appointment involved) calls both routes live. They were NOT dead code, so the fix had to keep that flow working, not just delete or lock the routes down.
**Was:** `GET /api/gift-cards/check/:code` had no auth and no store filter (any store's card, full row, by code alone). `POST /api/gift-cards/redeem` never checked the card belonged to the caller's store; was read-then-write (two stations redeeming at once could both pass the balance check); `amount` was unvalidated (a negative amount raised the balance; `NaN` slipped through).
**FIXED 2026-09-22:** both routes now resolve the store from the session (`resolveSessionStoreId`) instead of the request. `redeemGiftCard` in `lib/nailGiftCards.ts` (already built store-scoped/atomic/validated for the Nail POS) was generalized to take `appointmentId: number | null` — the Nail POS still passes a real ticket id (idempotent per ticket); the owner console passes `null` (no ticket to key off, so a repeat call is a fresh redemption, not treated as a retry). Both routes now delegate to it. Tests: a DB integration run (7 checks) covering the null-appointmentId path specifically — manual redeem, a second manual redeem NOT treated as a duplicate, overdraw refused, wrong store refused, negative amount refused, two simultaneous manual redeems both applying serialized by the row lock, one ledger row per call.

---

## 2026-09-21 — Checkout sheet (`CheckoutPOSPanel`): visible buttons that do nothing / drop data

**Found while:** reviewing the checkout sheet's function buttons on request (layout in `lib/pos/configs/nailSalon.ts`, dispatch in `pages/Calendar.tsx` `handlePosAction` ~7654-7785). Nothing was changed.
1. **Gift Card (Sell / Redeem / Check Balance) and Reprint (Last / Select… / Gift Receipt) are stubs.** `handlePosAction` sends `gift-card`, `membership`, `reprint-receipt` to `showPosStatus(tSt.comingSoon(...))` — the cashier just sees "GIFT CARD — COMING SOON". They are still drawn as normal live buttons (Gift Card tile, Reprint in the keypad row), and Reprint is also in the mobile action list.
2. **The "Employee" discount's reason is thrown away.** `nail.disc.employee` = `discount-preset { percent: 25, reason: "Employee" }`, but the `discount-preset` case only reads `percent`/`amount`; `payload.reason` is never used or saved, so nothing on the ticket/report says a 25% discount was an employee discount. (25% is also hard-coded in the layout file, not a setting.) Discounts of any size, including Comp (100%), need no manager PIN.
3. **No Sale leaves no record.** `no-sale` → `kickDrawer()` → `openCashDrawerHardware()` (`lib/cashDrawer.ts`) only pulses the drawer; no server call, event or cash-drawer-session entry, so drawer-open-without-a-sale can't be audited at Day Close.
4. ✅ **FIXED 2026-09-22 — Quick Ticket's icon was blank.** `nail.quickticket` uses `icon: "Zap"` (`lib/pos/configs/nailSalon.ts`) but `Zap` wasn't in `lib/pos/iconMap.ts`, so `resolvePosIcon` returned nothing and the tile showed a "•". `Zap` added to the import and `POS_ICONS` in `iconMap.ts` — one-line, no decision needed.
**Why 1-3 not fixed:** items 1-2 need product decisions (what gift cards / reprints should do, whether discounts need approval + a reason field), and item 3 touches the cash-drawer session model — unchanged from the original review.

## 2026-09-20 — Stripe M2 / Terminal card-payment path: audit findings (items 1-9 FIXED the same day; see status below)

**STATUS (same day):** items 1-9 below were fixed — server (`routes/stripeConnect.ts` capture/create/location, `lib/terminalPaymentMath.ts`, Connect webhook `payment_intent.succeeded`), checkout sheet (`Calendar.tsx`), owner app (`lib/captureRecovery.ts`, `useTerminalPayment.ts`, `useReaderDiscovery.ts`, `M2PaymentOverlay.tsx`, `ReaderStatusModal.tsx`) — and also brought in line with Stripe's docs (re-use the same PaymentIntent after a decline/timeout, show reader prompts + update progress). **Still open:** (a) the Stripe dashboard's Connect webhook endpoint must be subscribed to `payment_intent.succeeded` for the reconciliation handler to fire; (b) refunds/disputes of POS payments are still not reflected on appointments; (c) the app bundles Terminal Android SDK 5.5.1 — `@stripe/stripe-terminal-react-native@0.0.1-beta.33` bundles 5.8.0, which fixes "mobile reader software updates timing out on slow networks" (relevant to a new M2's first connect), but upgrading needs a lockfile + SDK patch change and a device test; (d) a group-pay ticket paid by M2 is recorded on the primary appointment at capture and corrected to each ticket's share when staff complete it.

**Found while:** reviewing `apps/certxa-owner` + the Checkout sheet (`CheckoutPOSPanel`) + `routes/stripeConnect.ts` for M2 payment wiring. Server-side account handling is correct (connection token, location, PaymentIntent all use `stripeAccount: <store's connected acct>`; `card_present`, manual capture, $0.60 application fee). Problems, most serious first:
1. **Failed capture is reported as success.** `apps/certxa-owner/lib/useTerminalPayment.ts:80,185` `await captureOnServer(...)` never checks the response, and the WebView RPC bridge deliberately resolves non-2xx bodies (`app/index.tsx:296`). A capture 500 (`{error}`) → `run()` returns → `M2PaymentOverlay` calls `onComplete` → web marks the ticket paid while the card is only authorized (hold later expires; money never collected).
2. **M2/Tap payments lose tip + discount (+ loyalty redemption, group pay).** Native path only PATCHes `status, paymentMethod, totalPaid` (`Calendar.tsx` ~1516-1535 handler + server capture `stripeConnect.ts:1118`), unlike `handleFinalizePayment` (`Calendar.tsx:1616`) which also saves `tipAmount`, `discountAmount`. `payrollRuns.ts:196-199` then counts the tip inside service revenue (commission overstated) and shows tips = 0; turn-queue threshold also counts the tip.
3. **Split tender under-records:** capture sets `totalPaid = pi.amount/100` (card portion only), so cash + card tickets are completed with only the card amount.
4. **No reconciliation webhook** for POS PaymentIntents: `stripeConnectWebhook.ts:222-234` handles only account/capability/deauthorize/transfer.reversed; `stripeWebhook.ts` `handlePaymentIntentSucceeded` ignores `source: certxa_pos`. If capture succeeds but the response/DB write is lost, nothing marks the appointment paid; refunds/disputes are never reflected.
5. **Capture route hardening:** no "already captured → return success" handling (a retried capture 500s though money moved); `create-payment-intent` trusts client-supplied `appointmentId` (`stripeConnect.ts:891`) with no check it belongs to the store, and capture then completes that appointment by id without a store scope.
6. **SDK init is one-shot** (`app/_layout.tsx:42-105`): if `initialize()` fails (e.g. Stripe not yet connected, early 401, slow WebView beyond ~12s) nothing retries until app restart.
7. **Retry after an error creates a fresh PaymentIntent** (`M2PaymentOverlay.tsx:165`), so an earlier authorized-but-uncaptured PI (finding 1's case) would double-hold the card. The error screen also says "DECLINED" (`:189`) for every failure (Bluetooth off, reader not found, etc.).
8. **Reader-registration UI/endpoint don't fit M2:** `ReaderStatusModal.tsx:120-146,234` + `stripeConnect.ts:1235` use a `registration_code` (for internet readers); Bluetooth M2 readers are attached via `connectReader({locationId})`, which the app already does. Confirm against Stripe docs, then remove or relabel.
9. **Placeholder Terminal Location address** (`stripeConnect.ts:829` and `:1271`: "123 Main St / Unknown / CA / 00000") is created and cached forever if the store address is blank; it is never updated after the owner fixes the address.
**Why not fixed now:** the task was to audit; items 1-3 touch payments/payroll data and need a decision plus an on-device test with a real M2 reader.

---

## 2026-09-15 — DB-cached plan price ($9.00) doesn't match the live Stripe Price ($14.95) it's tied to

**File:** `subscription_plans` table, row `code='solo'` (`price_monthly_cents: 900`) vs. its `stripe_price_id_monthly` (`price_1TtSVmRDJayCN7TTfV9etfV7`), which Stripe reports as a real unit amount of $14.95/mo.
**Found while:** verifying the new embedded subscribe flow against real Stripe data — creating a test invoice against that price ID to check `latest_invoice.payment_intent` behavior surfaced `amount_due: 1495` for a plan the UI displays everywhere (`DashboardBilling.tsx`'s hardcoded `PLANS` array, `SubscriptionPage.tsx`'s DB-driven card) as $9/mo.
**Why not fixed now:** Unrelated to the checkout-embedding task; fixing it requires knowing which number is authoritative (was the Stripe Price changed after the DB was seeded, or is the DB stale?) — a business decision, not a code fix. Whoever picks this up should reconcile `subscription_plans.price_monthly_cents` with the actual Stripe Price unit amount for every plan with a `stripe_price_id_monthly`, not just `solo`.

**Update 2026-09-20 (checked against live Stripe, read-only) — it is wider than `solo`, and the billing UI disagrees with both:**

| plan | `subscription_plans` monthly / yearly | live Stripe Price monthly / yearly | `DashboardBilling.tsx` `PLANS` shows |
|---|---|---|---|
| solo | $9.00 / $90.00 | **$14.95 / $168.00** | $9 |
| professional | $45.95 / $468.00 | $45.95 / $468.00 (matches DB) | **$22** |
| elite | $79.00 / $875.00 | $79.00 / $875.00 (matches DB) | **$49** |

So Stripe would charge $14.95 for a plan the DB and billing UI advertise at $9, and $45.95 / $79 for plans the billing UI advertises at $22 / $49. There is currently exactly one store subscription row (trialing), so nobody has been charged a wrong amount yet. **Needs a decision:** which numbers are the real prices. Then either fix the Stripe Price (create a new one — Prices are immutable) or the DB + the hardcoded `PLANS` array (better: make `DashboardBilling` read the DB like `SubscriptionPage` does so there is one source).

---

## Resolved 2026-09-20 (kept as a one-line record; details are in git history)

- **api-server `typecheck` OOM** — script now runs tsc with a 6 GB heap (`package.json`); it completes in ~1 min and reports **0 errors**.
- **`timezone.test.ts`** (65 errors, two entries) — imports `describe/test/expect` from `vitest`; 24 tests pass.
- **api-server unit tests** — `vitest.config.ts` now sets a dummy `DATABASE_URL`, so `emailTicketSync.test.ts` (which imports `db.ts`) loads instead of failing the suite; all 5 files / 99 tests pass with or without the real env var, and unit tests can never reach a real database.
- **`storage.ts` `sql.identifier([table])`** and **`dead-seats.ts` `totalDeadSlotCount`** — already fixed in the tree by an earlier change (typecheck confirms).
- **React error #185 pattern** — all 92 `data: x = []` sites (57 files) now use a shared frozen `EMPTY_ARRAY` (`booking/src/lib/empty.ts`); none of them mutated the default in place.
- **`static.ts` stale `/robots.txt`** — hardcoded fallback removed; the real `dist/public/robots.txt` is served by `express.static`.
- **`POST /api/intelligence/growth-assistant`** — implemented (`routes/intelligence.ts`): answers only from the store's own numbers (revenue vs last month, clients, seat utilisation, growth score, 90-day leakage), returns `{reply, highlights, actions}` with actions restricted to a fixed list of real routes.
- **Plan-switch trial** — `computeTrialPeriodDays` returns no trial when the store already has an active/past-due/unpaid *paid* subscription. (The billing page already routes active customers through `/api/billing/change-plan`, so this closes the direct-API gap.) Unchanged on purpose: a first-ever checkout still gets the full default trial.
- **`PayoutAccountSettings.tsx`** — confirmed the owner-level Express/OAuth flow is the intended design (only *contractors* moved to Custom accounts; see the stripe-connect-payouts skill).
- **WebSocket `ERR_NAME_NOT_RESOLVED`** — not a code bug: nginx proxies `/ws` and a fresh HTTP/1.1 upgrade to `/ws/notifications` returns 101 today; it was the tester's environment.
- **Owner-app WebView reload after login** — `StripeTerminalProvider` is now always mounted (only `TerminalInitializer` waits for login), so the screen tree/WebView is no longer remounted. **Needs an on-device check** (M2 connect + payment still work, no second `/app-login` load) — shipped in the next APK.

---

## Resolved 2026-09-21

- **Kiosk demo images** — the three dead `<img>` records (screens 3, 4, 6) are removed from `php/checkin-kiosk/default.php`; the demo now runs welcome → stylist → a CSS-drawn "You're checked in" confirmation (checked live in a browser).
- **"Open now" badge** — removed from both marketplace cards and the dead `isOpen` fields deleted; marketplace client + SSR bundles rebuilt and live.
- **`bufferTime`** — now a real setting: Calendar Settings → "Time between appointments" (None / 5 / 10 / 15 min), stored in `calendar_settings.buffer_minutes` (migration 0195, applied). Enforced in the booking engine, staff create route, reschedule, online/AI/staff availability lists, precomputed slots, auto-assign and the nail walk-in flow; both onboarding flows now save it (the setup-hub flow's save call and online-booking step were also broken and are fixed).
- **Suspended accounts vs Packages/Deals** — the page gate already blocked them; the server now also refuses `/api/packages` and `/api/deals` for suspended/locked stores, and the public marketplace hides a suspended store's deals and refuses checkout.
- **Commission rule (decided 2026-09-21: services + add-ons at the service rate; products only at the product rate)** — one shared function, `shared/commissionBasis.ts`, now drives the accrual ledger, payroll runs (new and legacy), contractor payout runs, the Commission report, the Salon Earnings report and the staff-portal earnings card. The checkout freezes the service/product split on each ticket (`appointments.service_revenue` / `product_revenue`, migration 0196; retail lines are the "retail" kind). Fixes along the way: add-ons were being paid twice in payroll (service rate inside the total *and* the product rate again) and were the only thing the "product rate" ever applied to; retail money was never recorded, so a product rate could not be honoured; the ledger now refreshes a still-pending amount when a ticket is completed a second time (card-reader capture completes first, the checkout sheet's split arrives second). Old tickets keep the legacy amount (everything paid, pre-discount, minus tip, at the service rate) — nothing was backfilled. `/pos` sales send retail money too. Not changed: sales tax is not in the new basis (it was inside the legacy one).
