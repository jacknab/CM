attached_assets/
.env
.env.*
!.env.example
node_modules/
dist/
# Template pre-built output must be committed — VPS serves these directly, no build step there
!templates-storage/nail-salon-bloom/project/dist/
!templates-storage/nail-salon-bloom/project/dist/**
!templates-storage/nail-salon-lacquer/project/dist/
!templates-storage/nail-salon-lacquer/project/dist/**
.next/
build/
*.log
logs/
.DS_Store
coverage/
.turbo/
.cache/
.local/
*.tsbuildinfo
# ecosystem.config.cjs is committed (no secrets — env vars injected via /etc/certxa.env + --update-env)

# Redis runtime dump — regenerated automatically, never commit
dump.rdb

# Tooling caches / runtime dirs — never belong in the repo
.npm/
.ngrok3
.config/
.pm2/
.claude/

# Nested skills clone (has its own .git)
geo-seo-claude/

# Package-manager stores / aborted filter-branch scratch / agent scratch output
.pnpm-store/
.git-rewrite/
.agents/outputs/
