# Robots.txt for Certxa — https://certxa.com
# Public marketing and booking pages are open to all crawlers.
# Internal app pages (dashboard, admin, staff tools) are disallowed.

User-agent: *
Allow: /
Crawl-delay: 2

# Internal app sections — not for public indexing
Disallow: /api/
Disallow: /includes/
Disallow: /config.php
Disallow: /router.php

# ── Block the visual editor (has noindex — keep Googlebot from discovering it) ─
Disallow: /editor

# ── Block launchsite internal paths — only the overview page should be indexed ─
Disallow: /launchsite/admin
Disallow: /launchsite/editor
Disallow: /launchsite/preview
Disallow: /launchsite/scraped-tmp/
Disallow: /launchsite/api/
Disallow: /launchsite/barbershops
Disallow: /launchsite/hair-salons

# ── Raw data files — never serve to crawlers ──────────────────────────────────
Disallow: /salon-data.json

# ── Known scraper / data-harvesting agents — block from the salon listings ────
# NOTE: individual salon pages are flat at root (certxa.com/{slug}, matching
# Vagaro's URL structure) so they can't be targeted by a robots.txt prefix
# without also disallowing real marketing pages — only the /listings/ state
# and city hub pages can be blocked this way. The sitemap rate limiter
# (routes/salonDirectory.ts, sitemapRateLimit) is the actual backstop against
# bulk harvesting of the flat listing pages themselves.
User-agent: AhrefsBot
Disallow: /listings/

User-agent: SemrushBot
Disallow: /listings/

User-agent: MJ12bot
Disallow: /listings/

User-agent: DotBot
Disallow: /listings/

User-agent: BLEXBot
Disallow: /listings/

User-agent: DataForSeoBot
Disallow: /listings/

User-agent: PetalBot
Disallow: /listings/

User-agent: Bytespider
Disallow: /listings/

# ── Search-engine crawlers — no crawl-delay (override the wildcard above) ────
User-agent: Googlebot
Allow: /
Crawl-delay: 0

User-agent: Bingbot
Allow: /
Crawl-delay: 0

User-agent: Slurp
Allow: /
Crawl-delay: 0

# Submit one canonical sitemap index; it references the marketing, blog, and salon
# child sitemaps without creating multiple submission entry points.
Sitemap: https://certxa.com/sitemap.xml
