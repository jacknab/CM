- [Template Booking Integration](template-booking-integration.md) — BookingFlow.tsx in template_master is the native booking engine; must stay in sync with MobileTheme.tsx.
- [Timezone Architecture](timezone-architecture.md) — TIMESTAMPTZ (migration 0097), TimeService, toZonedTime→getUTC* rule, frontend timezone.ts, all fixed files.
- [bookingEngine timezone fix](bookingengine-timezone-fix.md) — toSalonDateKey + business-hours check used toZonedTime+getUTC* (broken in v3); fixed to formatInTimeZone throughout.
- [date-fns-tz v3 toZonedTime broken](date-fns-tz-v3-tozonedtime.md) — getUTCHours() on toZonedTime result returns UTC hour in v3; always use formatInTimeZone instead.
- [authenticatedPaths list](authenticated-paths.md) — App.tsx gates StoreProvider on this list; any new page that needs selectedStore MUST be added here or toggles/store-scoped data silently fail.
- [Offline-first booking system](offline-first.md) — appointments cache, local clients, action queue all wired; critical hooks updated.
- [Staff password column removal](staff-password-removal.md) — staff table has no password column; 9 stale code spots to clean up after any staff schema refactor.
- [Staff Mobile OTP auth shape mismatch](staff-mobile-otp-auth.md) — OTP returns staffId/firstName/lastName/profileImageUrl; normalizeStaffUser() maps to StaffUser; always call before setUser.
- [tanstack/query-core Hermes fix](tanstack-hermes-fix.md) — patch package.json exports → legacy build in startup script; Metro exports default broke in Expo SDK 56.
- [Stack overview](stack.md) — pnpm monorepo: API server on port 9200 (Express/Drizzle/Postgres), booking app on port 5000 (Vite/React); shared schema in shared/schema.ts.
- [Contractor Payouts system](contractor-payouts.md) — full payout/direct-deposit system built; DB tables, API routes, 8 frontend pages all wired.
- [Check Printer — pre-printed paper](check-printer-preprinted.md) — paperType="preprinted" renders text-only on 8.26"×10.76" blue security paper; zones 3.44/3.44/3.88"; suppress security bands + transparent bg in print.
- [Payroll Scheduler](payroll-scheduler.md) — hourly scheduler auto-creates draft payout_runs at period close and auto-approves after review window; helpers createPayoutRunForPeriod/approvePayoutRunById exported from contractorPayouts.ts; payout tables missing from fresh DB — must apply via raw SQL.
- [Booking Engine](booking-engine.md) — unified scheduling authority in bookingEngine.ts; all booking paths route through it.
- [Replit dev workflow](replit-dev-workflow.md) — "Start application" workflow runs both API (PORT=9200) and Vite (PORT=5000); the separate "artifacts/api-server: API Server" workflow binds a DIFFERENT port — always restart "Start application" to deploy API changes to the Vite-proxied port.
- [Redis availability cache](redis-availability-cache.md) — ioredis + BullMQ wired into AI receptionist; gracefully no-ops when REDIS_URL unset; activate by setting REDIS_URL on VPS.
- [Transactional email system](transactional-emails.md) — all platform emails in lib/systemEmails.ts; uses sendEmail from mail.ts with MAILGUN_API_KEY + MAILGUN_DOMAIN env vars.
- [Email Preference Centre](email-preferences.md) — user_email_preferences table (VARCHAR user_id — users.id is UUID/varchar not int); API at /api/settings/email-preferences; UI card at bottom of /mail-settings.
- [users.id is VARCHAR/UUID](users-id-type.md) — users table id column is character varying (UUID via gen_random_uuid()), NOT integer. Any FK to users must use VARCHAR/text, not integer.
- [Support back-office migration baseline trap](support-backoffice-migration.md) — migrations/0031 was seeded as baseline (not executed) on Replit; tables must be applied manually then schema fixes captured in 0032.
- [Feature-Based Subscription System](feature-subscription-system.md) — dynamic plan/feature/limits system; no hardcoded plan checks; all entitlements via DB; key files: featureAccess.ts, routes/plans.ts, routes/subscription.ts, SubscriptionPage.tsx, UpgradeBlockModal.tsx.
- [NULL::INTEGER cast in UNION ALL](sql-union-null-cast.md) — when mixing NULL and integer literals across UNION ALL branches, always cast as NULL::INTEGER or PostgreSQL infers text and throws a type error.
- [Stripe Billing & Wallet System](stripe-billing.md) — lazy Stripe singleton (Proxy pattern), store-scoped billing, 3 new DB tables, webhook at /api/stripe/webhook; needs 3 env vars to activate.
- [SMS billing model](sms-billing-model.md) — deduction order: smsAllowance (monthly sub, no rollover) → platformCredits wallet ($0.02/SMS, atomic WHERE COALESCE >= 0.02) → fail immediately + log; no smsCredits purchased bucket; resolveSmsAccess() reads smsAllowance directly (not plan feature system).
- [Certxa Support Agent](support-agent.md) — fully isolated voice support module; KB in artifacts/api-server/knowledge-base/ (15 .md files); DB tables applied via raw SQL (drizzle push is interactive); admin dashboard at /admin/support-agent.
- [Security audit: route ownership pattern](route-security-pattern.md) — all routes must use resolveSessionStoreId(req) not req.body/query.storeId; validateStoreOwnership had a critical bug (returned truthy Response, never blocked). Admin trial/subscription routes were completely unauthenticated — now gated by requireAdmin helper.
- [Staff login isolation](staff-login-isolation.md) — /api/auth/staff-login checks ONLY the staff table; shared /api/auth/login checks users table first and would accidentally route staff as owners if their email existed in both tables.
- [clients is the FK target — customers retired](clients-migration.md) — all 11 FK tables now point to clients(id); appointments.customer_id is unconstrained (Drizzle defines no FK there); customers table kept but never written to.
- [clients table columns](clients-missing-columns.md) — clients has loyalty_points + notes (added via ALTER TABLE); appointments has checked_in_at; staff has avatar_thumb_url (80×80 WebP thumb from R2).
- [R2 image pipeline](r2-image-pipeline.md) — lib/r2.ts: all uploads auto-converted to WebP via sharp; uploadAvatarToR2() also generates 80×80 thumb stored in staff.avatar_thumb_url; sharp approved in pnpm-workspace.yaml onlyBuiltDependencies.
- [Back-office dev auth bypass](backoffice-dev-bypass.md) — requireSupportAuth uses inverse env var: bypass is ON unless SUPPORT_REQUIRE_AUTH=true; NODE_ENV check does NOT work reliably in esbuild bundles — use opt-in production flag instead.
- [support_tickets table collision](support-tickets-collision.md) — original support_tickets table was the AI voice agent's schema (issue NOT NULL, no account_id/subject); back-office needed ALTER TABLE to add columns and DROP NOT NULL on issue; always seed with issue=NULL for back-office rows.
- [Kiosk Check-In System](kiosk-system.md) — GoCheckin-style tablet kiosk at /kiosk/:slug; public API at /api/public/kiosk/*; kiosk_checkins table auto-created at startup; QR ticket at /kiosk/:slug/ticket/:token.
- [Data retention policy](data-retention.md) — payroll/earnings data must never be deleted until Feb 1 of the following year; suspension/cancellation gates access only, never deletes rows.
- [Account status enforcement gaps](account-status-gaps.md) — suspension enforcement is frontend-only (AccountStatusGate) with a case-sensitivity bug; public booking endpoints and website serving have no server-side status check.
- [Missing DB columns pattern](missing-db-columns.md) — schema columns added after baseline must get a numbered migration in artifacts/api-server/migrations/; without it Drizzle explicit SELECTs crash prod. Fixed: language (calendar_settings 0042), illustration cols (services 0041), service_illustration_categories table (0041).
- [resolveSessionStoreId now validates query param](session-store-resolve.md) — sessionStore.ts now reads storeId from req.query/req.body, validates ownership against DB (never blind trust), falls back to first store; Calendar page uses ?storeId= query param so this fix was required for business hours/settings to resolve correctly.
- [Stripe Connect + Terminal M2](stripe-connect-terminal.md) — isolated payment system (never touches SaaS billing); store_payment_accounts table (migration 0044); Terminal SDK loaded from CDN; 3 env vars needed to activate; refetchInterval must use function form to avoid TDZ crash.
- [SMS Inbox & Multi-Tenant Routing](sms-inbox-routing.md) — shared toll-free number routes to correct salon via sms_contact_routing table (migration 0055); 3-step fallback: routing table → sms_log → client_phones/appointments; outbound reply updates routing record; inbound webhook at POST /api/webhooks/twilio/incoming.
- [Staff vs Contractor detail split](staff-contractor-split.md) — StaffDetail (/staff/members/:id) is Profile-only (name/email/phone/role/bio/photo/permissions/password-reset); Schedule/Services/MailingAddress tabs live in ContractorDetail (/payouts/contractors/:id); contractor.staffId bridges the two — tabs render nothing if staffId is null.
- [Live Chat public route bypass](live-chat-auth-bypass.md) — routes.ts has a global /api auth gate (line ~1020) that requires userId or staffId; add path.startsWith bypasses for /live-chat/ (public) and /support/live-chat/ (custom requireSupportAuth); migration 0057_live_chat.sql must be applied manually via psql.
- [Live Chat system](live-chat-system.md) — full WS chat at /ws/live-chat; agent dashboard at /isTeam/live-chat; customer page at /chat; features: queue, active, history, internal notes, inline / canned picker, agent Online/Away/Busy, typing indicators both ways, Web Audio notifications, browser Notification API, estimated wait time, visitor page URL, department transfer.
- [Live Chat smart routing + account widget](live-chat-account-widget.md) — migrations 0061 (routing) and 0062 (account_id); ChatWidget.tsx is the embeddable popup; /contact uses slide-in; GET /api/live-chat/me pre-fills session; account panel in agent Info tab via GET /api/support/live-chat/:id/account.
- [Account Timeline page](account-timeline.md) — Screen 7: /isTeam/account-timeline/:accountId; 3-panel layout; API at /api/support/accounts/:id/timeline (wraps fetchActivityEvents + support_tickets + billing_activity_logs); Customer360Page has "Timeline" shortcut button.
- [Billing Investigation page](billing-investigation.md) — Screen 6: /isTeam/billing-investigation + /:accountId; list view with search+filters, detail view has account bar + 9 tabs + KPI cards + customer sidebar + billing table + issue shortcuts; API at /api/support/billing/:accountId + 6 action endpoints already built.
- [Incidents & Service Health page](incidents-page.md) — Screen 5: /isTeam/incidents + /:incidentId; 4 tables auto-created at startup in support.ts bootstrap block; API: /kpi, /service-health, /trends, CRUD incidents+updates+tasks+postmortem; detail has Timeline/Tasks/Postmortem tabs.
- [SMS wallet NULL bug](sms-wallet-null.md) — platform_credits NULL + any amount = NULL in Postgres; all 4 broken write sites fixed with COALESCE; startup repair syncs from ledger for existing NULL rows.
- [Auto-close tickets scheduler](auto-close-tickets.md) — runAutoCloseTickets() exported from routes/support.ts; wired into index.ts via setInterval (hourly) + 30s startup delay; closes open/pending tickets where last non-internal message was outbound > 7 days ago with no inbound reply since; sends closure email via sendSupportReply.
- [TicketsPage URL param](tickets-url-param.md) — TicketsPage reads ?ticketId= from search params on mount and auto-opens that ticket; RecentTicketsCard navigates to /isTeam/tickets?ticketId=<id> to deep-link from Customer360 Tickets tab.
- [E.164 phone normalization](e164-normalization.md) — canonical utility at lib/phoneUtils.ts; sendSms(), kiosk checkin, missed-you SMS, noshow-waitlist, online booking, waitlist join all normalize to E.164 before storage/SMS; noshow-waitlist client lookup uses client_phones.phone_number_e164 not clients.phone.
- [drizzle-kit push clean state](drizzle-push-clean.md) — all 33 schema-only tables created, 24 unique constraints renamed _key→_unique, type mismatches fixed; push now runs silently. New tables added to schema must also be created in DB via raw SQL before running push.
- [Google OAuth token encryption](google-token-encryption.md) — AES-256-GCM via lib/googleTokenCrypto.ts; encryptToken() on every write, decryptToken() before setCredentials(); legacy plaintext auto-migrates on next write; GOOGLE_TOKEN_ENCRYPTION_KEY secret set.
- [Auto-mode website publisher](auto-website-publisher.md) — wb_websites has publisher_type + auto_settings columns (migration 0073); renderSalonPage() in render-salon-page.ts; serveAutoPage() in template-serve.ts; auto-preview at GET /api/websites/:id/auto-preview; website builder runs on port 4173.
- [Template preview builds](template-preview-builds.md) — seeded templates with no thumbnail must trigger screenshot builds; sanitize only an isolated copy, never mutate catalog source files.
- [Template VPS deployment boundary](template-vps-deployment.md) — local template source/dist changes do not affect certxa.com until the updated files are deployed to the VPS.
- [Template preview live data](template-preview-live-data.md) — builder previews wait for server salon context and inject the shared tenant payload before template bundles mount.
- [Service image media separation](service-image-media-separation.md) — reviewer avatars are metadata only; service-result images require actual review media or customer-uploaded photos.
- [Template Google Maps embeds](template-google-maps.md) — use the dedicated embed endpoint plus a direct Maps fallback; legacy `maps?q` embeds can go blank.
- [Multilingual Content Translations](multilingual-translations.md) — entity_translations table (migration 0074); triggerTranslation() is fire-and-forget after entity create/update; supports es/vi/zh/ko; UI at /settings/translations.
- [SERIAL sequence without OWNED BY](serial-sequence-no-owned-by.md) — pg_depend and pg_get_serial_sequence both silently return nothing; only pg_attrdef + LIKE 'nextval%' detects it; one-time SQL fix + deploy.sh updated.
- [Deploy.sh pre-push guard scripts](deploy-guard-scripts.md) — check-sequence-guard-sync + check-migration-drift added to catch tablesFilter/deploy.sh drift and baseline-seeding trap before push, not at server-startup crash time.
- [Workspace symlink exclusions](workspace-symlink-exclusions.md) — generated artifacts/scripts and artifacts/shared symlinks duplicate canonical pnpm packages; exclude them from artifacts/*.
- [Dev sandbox ESM resolution quirk](esm-resolution-stale-node-modules.md) — plain `node file.mjs` inside a pnpm workspace subpackage can transiently fail with ERR_MODULE_NOT_FOUND for real, installed deps; fix is `pnpm install` (not a code bug).
- [Kiosk check-in auto-cancel bug](kiosk-checkin-auto-cancel.md) — background dup-reconciliation job must never auto-cancel checked_in/started appointments; public creation endpoints need idempotency guards.
- [Imported preview bootstrap](imported-preview-bootstrap.md) — template thumbnail generation can delay the production preview beyond workflow startup limits.
- [Dashboard timezone display](dashboard-timezone-display.md) — format raw instants once; formatting a wall-clock Date again applies the store offset twice.
