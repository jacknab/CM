/**
 * Base URL for your backend. Confirmed from Replit: the connection
 * token route already lives at
 * https://certxa.com/api/payments/terminal/connection-token — so the
 * base is the main app's own domain, same-origin, no separate API
 * subdomain.
 */
export const BACKEND_BASE_URL = 'https://certxa.com/api/payments';
